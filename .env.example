# Configuración del Frontend (solo para desarrollo local)
# En producción, el frontend hace requests al mismo dominio donde está hosteado

# URL del backend (solo necesario para desarrollo local)
VITE_BACKEND_URL=http://localhost:8080
